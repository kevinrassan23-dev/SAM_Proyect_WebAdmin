/**
 * Aquí van las funciones de autenticación de firebase:
 * 
 * EJEMPLO: un método para verificar usuario y contraseña desde firebase ->
 * 
 * export const login = (user, password) =>
 * auth.verificarUserAndPassword(user, password);
 * export const logout = () => auth.signOut();
 */

import { auth } from '../../config/firebaseConfig';